# 🧪 دليل الاختبارات الشاملة - Faheemly

## 📋 نظرة عامة

هذا الدليل يشرح كيفية تشغيل الاختبارات الشاملة لنظام Faheemly بما في ذلك:
- ✅ اختبارات API
- ✅ اختبارات الويدجت
- ✅ اختبارات لوحة التحكم
- ✅ اختبارات الأداء
- ✅ اختبارات الأمان
- ✅ اختبارات التكامل

## 🚀 الإعداد الأولي

### 1. تثبيت المتطلبات

```bash
# في مجلد tests
cd tests
npm install

# تثبيت Playwright browsers
npx playwright install
```

### 2. إعداد متغيرات البيئة

```bash
# في ملف .env في مجلد tests
API_URL=http://localhost:3001/api
WEB_URL=http://localhost:3000
```

## 🧪 تشغيل الاختبارات

### اختبارات API (Unit Tests)

```bash
# في مجلد api
cd ../api
npm test

# أو تشغيل اختبارات محددة
npm test -- --testNamePattern="Chat API"
```

### اختبارات E2E (End-to-End)

```bash
# في مجلد tests
cd tests

# تشغيل جميع الاختبارات
npm test

# تشغيل مع واجهة مرئية
npm run test:ui

# تشغيل مع عرض المتصفح
npm run test:headed

# تشغيل في وضع التصحيح
npm run test:debug

# تشغيل على متصفح محدد
npx playwright test --project=chromium
npx playwright test --project=firefox
npx playwright test --project=webkit
```

### تشغيل جميع الاختبارات

```bash
# من مجلد tests
npm run test:all
```

## 📊 عرض النتائج

```bash
# عرض تقرير HTML
npm run test:report

# سيتم فتح التقرير في المتصفح تلقائياً
```

## 🎯 الاختبارات المتوفرة

### 1. اختبارات المصادقة (Auth)
- ✅ التسجيل
- ✅ تسجيل الدخول
- ✅ التحقق من الجلسة
- ✅ رفض البيانات غير الصحيحة

### 2. اختبارات الدردشة (Chat)
- ✅ إرسال الرسائل
- ✅ استقبال ردود البوت
- ✅ الاحتفاظ بسياق المحادثة
- ✅ معالجة الأخطاء

### 3. اختبارات التقييمات (Rating)
- ✅ تقييم المحادثات
- ✅ التحقق من صحة التقييمات
- ✅ حفظ التقييمات

### 4. اختبارات قاعدة المعرفة (Knowledge Base)
- ✅ إضافة معرفة
- ✅ تحديث المعرفة
- ✅ حذف المعرفة
- ✅ استخدام البوت للمعرفة

### 5. اختبارات الويدجت (Widget)
- ✅ تحميل الويدجت
- ✅ فتح نافذة الدردشة
- ✅ إرسال رسائل
- ✅ حفظ الجلسة
- ✅ استعادة الجلسة بعد التحديث

### 6. اختبارات لوحة التحكم (Dashboard)
- ✅ تسجيل الدخول
- ✅ عرض الإحصائيات
- ✅ عرض المحادثات
- ✅ التنقل بين الصفحات

### 7. اختبارات الأداء (Performance)
- ✅ سرعة الاستجابة
- ✅ الطلبات المتزامنة
- ✅ تحمل الضغط

### 8. اختبارات الأمان (Security)
- ✅ الحماية من SQL Injection
- ✅ الحماية من XSS
- ✅ Rate Limiting
- ✅ التحقق من الصلاحيات

### 9. اختبارات استمرارية الجلسة (Session Persistence)
- ✅ حفظ الجلسة في localStorage
- ✅ استعادة الجلسة بعد التحديث
- ✅ حفظ رسائل المحادثة

### 10. اختبارات التكامل الشاملة (Integration)
- ✅ سيناريو كامل من التسجيل إلى التقييم
- ✅ تكامل جميع المكونات

## 🔍 تصحيح الأخطاء

### عرض تفاصيل الفشل

```bash
# تشغيل مع traces
npx playwright test --trace on

# تشغيل مع screenshots و videos
npx playwright test --screenshot on --video on
```

### فحص اختبار محدد

```bash
# تشغيل اختبار واحد
npx playwright test -g "يجب أن يرسل رسالة"

# تشغيل ملف اختبار واحد
npx playwright test tests/e2e/comprehensive.spec.ts
```

## 📈 معايير النجاح

### الأداء
- ⏱️ استجابة API: < 2 ثانية
- ⏱️ تحميل الصفحة: < 3 ثواني
- 🔄 طلبات متزامنة: 10+ طلبات في نفس الوقت

### التغطية
- 📊 تغطية الكود: > 80%
- ✅ اختبارات ناجحة: 100%
- 🐛 أخطاء: 0

## 🛠️ أدوات إضافية

### تشغيل في CI/CD

```bash
# في GitHub Actions أو Jenkins
CI=true npm test
```

### إنشاء تقرير JSON

```bash
npx playwright test --reporter=json
```

### تشغيل على أجهزة متعددة

```bash
# Desktop
npx playwright test --project="Desktop Chrome"
npx playwright test --project="Desktop Firefox"
npx playwright test --project="Desktop Safari"

# Mobile
npx playwright test --project="Mobile Chrome"
npx playwright test --project="Mobile Safari"
```

## 🔧 حل المشاكل الشائعة

### المشكلة: الاختبارات تفشل بسبب timeout

**الحل:**
```typescript
// زيادة timeout في playwright.config.ts
timeout: 60000, // 60 ثانية
```

### المشكلة: البيانات لا تُحذف بعد الاختبار

**الحل:**
```typescript
// استخدام afterAll hook
afterAll(async () => {
  // cleanup code
});
```

### المشكلة: الاختبارات تتعارض مع بعضها

**الحل:**
```bash
# تشغيل بشكل تسلسلي
npx playwright test --workers=1
```

## 📝 إضافة اختبارات جديدة

### مثال: إضافة اختبار جديد

```typescript
test('اسم الاختبار', async ({ request }) => {
  const response = await request.post(`${API_URL}/endpoint`, {
    data: { /* your data */ }
  });
  
  expect(response.ok()).toBeTruthy();
  const data = await response.json();
  expect(data.field).toBe(expectedValue);
});
```

## 🎨 أفضل الممارسات

1. **استخدم أسماء وصفية**: اجعل أسماء الاختبارات واضحة ومفهومة
2. **احذف البيانات التجريبية**: استخدم `afterAll` أو `afterEach` للتنظيف
3. **اختبر سيناريوهات حقيقية**: اكتب اختبارات تحاكي استخدام المستخدمين الفعليين
4. **استقلالية الاختبارات**: كل اختبار يجب أن يعمل بشكل مستقل
5. **استخدم fixtures**: لتجنب تكرار الكود

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من السجلات في `test-results/`
2. افتح issue في GitHub
3. راجع [Playwright Documentation](https://playwright.dev)

---

**آخر تحديث:** 21 ديسمبر 2025
**الإصدار:** 1.0.0
