// Backwards compatibility: forward to analytics.routes
// Thin shim to preserve older imports/tests that reference `conversation-analytics.routes.js`.
export { default } from './analytics.routes.js';

