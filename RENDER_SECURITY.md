# 🔒 حماية بيانات Redis وإعدادها على Render

لقد قمت بتحديث ملف `.env` المحلي لديك ليعمل المشروع الآن على جهازك. ولكن لحماية هذه البيانات عند الرفع على الاستضافة (Render)، يجب عليك اتباع الخطوات التالية بدلاً من رفع ملف `.env`.

## 1. عدم رفع ملف .env
تأكد من أن ملف `.env` موجود في ملف `.gitignore` (وهو كذلك عادةً) حتى لا يتم رفعه إلى GitHub ويراه الجميع.

## 2. إضافة المتغير في Render
عند عمل Deploy للمشروع على منصة Render، اتبع الخطوات التالية لإضافة رابط Redis بشكل آمن:

1. اذهب إلى لوحة التحكم في **Render**.
2. اختر مشروع السيرفر الخاص بك (**Web Service**).
3. من القائمة الجانبية، اضغط على **Environment**.
4. اضغط على زر **Add Environment Variable**.
5. أضف البيانات التالية:
   - **Key:** `REDIS_URL`
   - **Value:** `redis://default:rkSQTYTGg3xpnP8Fm8XMnGw5RSr0kUek@redis-12651.c253.us-central1-1.gce.cloud.redislabs.com:12651`

   - **Key:** `DATABASE_URL`
   - **Value:** `postgresql://fahimo_user:ugbqfF41eTQkwCTqYum8wJi9Y3GTh0Fq@dpg-d4ni1bfpm1nc73e7e5gg-a.oregon-postgres.render.com:5432/fahimo?schema=public&ssl=true`

## 3. متغيرات أخرى مهمة
تأكد أيضاً من إضافة باقي المتغيرات الحساسة بنفس الطريقة في Render:
- `DATABASE_URL`
- `JWT_SECRET`
- `GROQ_API_KEY`
- `GEMINI_API_KEY`

بهذه الطريقة، تظل بياناتك مشفرة وآمنة داخل بيئة Render ولا تظهر في الكود المصدري.
