# IMPORTANT:
# Next.js loads `.env.local` in *all* environments (including production builds).
# To prevent accidentally baking localhost into the static export, keep `.env.local`
# production-safe and put dev-only values in `.env.development.local`.

NEXT_PUBLIC_API_BASE_URL=https://fahimo-api.onrender.com/api
NEXT_PUBLIC_API_URL=https://fahimo-api.onrender.com/api
NEXT_PUBLIC_SOCKET_URL=https://fahimo-api.onrender.com
NEXT_PUBLIC_APP_ENV=production
